(executable
  (name benchmark)
  (modules Benchmark Measure Database)
  (libraries deques unix sek)
  (flags (:standard -w -26-32-33)))

(executable
  (name listLike)
  (modules ListLike)
  (libraries deques unix))
