(executable
  (name benchmark)
  (public_name benchmark)
  (package deques)
  (modules Benchmark Base)
  (libraries deques unix))

(executable
  (name listLike)
  (public_name list_like_benchmark)
  (package deques)
  (modules ListLike)
  (libraries deques unix))
